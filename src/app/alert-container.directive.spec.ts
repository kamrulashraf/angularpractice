import { AlertContainerDirective } from './alert-container.directive';

describe('AlertContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertContainerDirective();
    expect(directive).toBeTruthy();
  });
});
